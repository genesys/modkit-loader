declare module 'es6-template-strings' {
  /**
   * Templates as an es6 litteral.
   * @param tpl The string template.
   * @param context The context.
   */
  export default function template (tpl: string, context: any): string;
}
